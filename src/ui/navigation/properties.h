#pragma once

#include <glad/glad.h>
#include <GLFW/glfw3.h>
#include "imgui.h"

namespace UI {
	class Properties {
	public:
		Properties() = default;

		// TODO
		// Properties stay at right side of the screen! (wide)

	};
}